apiVersion: v1
kind: Service
metadata:
  name: doks-flask
  namespace: dev
  labels: { app: doks-flask }
spec:
  selector: { app: doks-flask, role: primary }
  ports:
    - port: 80
      targetPort: http
      name: http
