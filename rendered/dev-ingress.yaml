apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: doks-flask
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: dev.165.227.242.33.sslip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: doks-flask
            port:
              number: 80
